# discord.py 2.7+ tiene soporte nativo para el protocolo DAVE/E2EE (obligatorio desde marzo 2026)
discord.py[voice]>=2.7.0

# Voice receive con soporte DAVE: usamos el fork del PR #56 (oficial pendiente de merge)
discord-ext-voice-recv @ git+https://github.com/vocolboy/discord-ext-voice-recv@main

faster-whisper>=1.0.0
ollama>=0.2.0
python-dotenv>=1.0.0
davey
PyNaCl>=1.5.0

# Librerías CUDA requeridas por faster-whisper / CTranslate2 para inferencia en GPU
nvidia-cublas-cu12
nvidia-cudnn-cu12==9.*

# TTS: síntesis de voz con modelos VITS de Hugging Face (facebook/mms-tts-spa)
transformers>=4.42.0
# torch: instalar con soporte CUDA desde https://pytorch.org/get-started/locally/
# si se necesita GPU para TTS. La versión CPU se instala automáticamente como dep de transformers.
torch>=2.2.0

# Wrapper Python de FFmpeg (complementario al binario ffmpeg en PATH)
ffmpeg-python>=0.2.0
